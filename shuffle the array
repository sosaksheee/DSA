class Solution:
    def shuffle(self, nums: list[int], n: int) -> list[int]:
        l1=nums[:n]
        l2=nums[n:]
        l3=[]
        for i in range(n):
            l3.append(l1[i])
            l3.append(l2[i])
        return l3
s=Solution()
l=s.shuffle([1,1,2,2],2)
print(l)
